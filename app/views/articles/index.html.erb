<h1>This is my index page</h1>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>description</th>
      <th>placeholder</th>
    </tr>
  </thead>
  <tbody>
    <% @article.each do |item| %>
    
    <tr>
      <td><%= item.title%></td>
      <td><%= item.description%></td>
      <td><%= link_to 'Delete', article_path(item), data: {turbo_method: :delete, turbo_confirm: "are you sure"} %>
        <%= link_to 'Show', article_path(item) %>
        <%= link_to 'Edit', edit_article_path(item) %></td>
    </tr>
    <% end %>
  </tbody>
  
</table>
<p>
   <%= link_to 'Create new article', new_article_path %>
</p> 